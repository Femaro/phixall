# US site media

Place optional video assets here for service pages and the homepage.

- **`hero-placeholder.mp4`** — Referenced by engineering and automation pages. Until this file exists, pages use poster images via `<video poster="...">` so visuals still load.
- Add matching posters as **`poster-*.jpg`** if you replace remote poster URLs.

Replace files without renaming so routes keep working.
